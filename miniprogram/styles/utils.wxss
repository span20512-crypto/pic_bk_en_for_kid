/* ============================================================
 * 原子类对照表（PRD §1.1）
 * 单位一律用 px，不随屏宽缩放，与设计稿一致。
 * 主题色：奶油白 #FFFDF7 / 森林绿 #34D399 / 活力橙 #FF8C42
 *         Level 1 绿 #22C55E / Level 2 黄 #FBBF24
 * ============================================================ */

/* ---- 布局 ---- */
.flex        { display: flex; }
.flex-col    { display: flex; flex-direction: column; }
.items-center{ align-items: center; }
.justify-center { justify-content: center; }
.justify-between { justify-content: space-between; }
.flex-1      { flex: 1; }
.wrap        { flex-wrap: wrap; }
.relative    { position: relative; }
.absolute    { position: absolute; }
.hidden-el   { display: none; }

/* ---- 间距 ---- */
.p-8  { padding: 8px; }
.p-12 { padding: 12px; }
.p-16 { padding: 16px; }
.px-16 { padding-left: 16px; padding-right: 16px; }
.py-8  { padding-top: 8px; padding-bottom: 8px; }
.mt-4  { margin-top: 4px; }
.mt-8  { margin-top: 8px; }
.mt-12 { margin-top: 12px; }
.mt-16 { margin-top: 16px; }
.mb-8  { margin-bottom: 8px; }
.mb-12 { margin-bottom: 12px; }
.mr-8  { margin-right: 8px; }
.gap-8  { gap: 8px; }
.gap-12 { gap: 12px; }

/* ---- 文字 ---- */
.text-12 { font-size: 12px; }
.text-13 { font-size: 13px; }
.text-14 { font-size: 14px; }
.text-15 { font-size: 15px; }
.text-16 { font-size: 16px; }
.text-18 { font-size: 18px; }
.text-20 { font-size: 20px; }
.text-24 { font-size: 24px; }
.bold     { font-weight: 600; }
.bolder   { font-weight: 700; }
.text-center { text-align: center; }

/* ---- 颜色 ---- */
.c-ink    { color: #1F2937; }
.c-sub    { color: #6B7280; }
.c-faint  { color: #9CA3AF; }
.c-green  { color: #34D399; }
.c-orange { color: #FF8C42; }
.c-white  { color: #FFFFFF; }
.bg-cream  { background: #FFFDF7; }
.bg-white  { background: #FFFFFF; }
.bg-green  { background: #34D399; }
.bg-orange { background: #FF8C42; }

/* ---- 卡片 ---- */
.card-base {
  background: #FFFFFF;
  border-radius: 16px;
  box-shadow: 0 4px 14px rgba(31, 41, 55, 0.06);
}
.round-8  { border-radius: 8px; }
.round-12 { border-radius: 12px; }
.round-16 { border-radius: 16px; }
.round-full { border-radius: 999px; }

/* ---- 等级徽章 ---- */
.badge-level {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 999px;
  font-size: 11px;
  font-weight: 600;
  color: #FFFFFF;
}
.badge-level-1 { background: #22C55E; }
.badge-level-2 { background: #FBBF24; }

/* ---- 点按态（原生 hover-class，PRD §1.1）---- */
.press {
  transform: scale(0.98);
  box-shadow: 0 2px 6px rgba(31, 41, 55, 0.05);
}
.press-dim {
  opacity: 0.75;
}
